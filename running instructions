python app.py

^
Just run that in terminal with python installed 
and any other relevant dependancies like boto3
Then go to localhost://5000

Also for AWS tasks run:

aws configure

and add your credentials every time a new lab session starts. 